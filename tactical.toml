# Public aggressive paper experiment. No credentials or account access.
trial_id = "tactical-20260915"
universe_size = 8
minimum_quote_volume_usdt = 20000000
minimum_listing_days = 30
scan_seconds = 120
poll_seconds = 15
leverages = [1, 2, 3]
capital_usdt = 50.0
max_loss_fraction = 0.50
max_drawdown_fraction = 0.20
daily_loss_fraction = 0.05
risk_per_trade_fraction = 0.02
max_margin_fraction = 0.60
fee_bps = 5.0
slippage_bps = 4.0
maintenance_margin_fraction = 0.02
max_spread_bps = 20.0
max_quote_age_seconds = 30
max_signal_age_seconds = 180
max_review_age_seconds = 90
max_holding_minutes = 120
take_profit_r = 2.0
max_testnet_deviation_fraction = 0.01
